spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://localhost:3306/test_task
    username: root
    password:
    hikari:
      idle-timeout: 10000
      maximum-pool-size: 20
      minimum-idle: 10
server:
  port: 8081